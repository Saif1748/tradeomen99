// src/data/trades.ts
/**
 * Stub file — satisfies the `@/data/trades` import in widgetRegistry.tsx.
 * All widgets now receive live data via WidgetRenderer props instead of
 * reading from a static sample array.
 */
import { Trade } from "@/types/trade";

export const sampleTrades: Trade[] = [];
